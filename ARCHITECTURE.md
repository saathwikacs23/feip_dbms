# 🏗️ SYSTEM ARCHITECTURE DIAGRAMS
## Visual Guide to Federated Environmental Intelligence Platform

---

## 📊 HIGH-LEVEL ARCHITECTURE

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                             │
│                    (Chrome, Firefox, Edge)                       │
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ Login Page   │  │  Dashboard   │  │ Query Page   │          │
│  │  HTML/CSS/JS │  │  HTML/CSS/JS │  │  HTML/CSS/JS │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ HTTP REST API
                             │ (JSON)
                             │
┌────────────────────────────▼────────────────────────────────────┐
│                     FLASK BACKEND SERVER                         │
│                         (Python)                                 │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  app.py - Main Application & Routing                      │  │
│  ├──────────────────────────────────────────────────────────┤  │
│  │  auth.py - Authentication & Authorization                 │  │
│  ├──────────────────────────────────────────────────────────┤  │
│  │  database.py - Database Connection Management             │  │
│  ├──────────────────────────────────────────────────────────┤  │
│  │  config.py - Configuration Settings                       │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                   │
│  ┌────────────────────┐           ┌────────────────────┐        │
│  │ Session Management │           │  Query Logging     │        │
│  │   (Flask Session)  │           │   (PostgreSQL)     │        │
│  └────────────────────┘           └────────────────────┘        │
└──────────────┬──────────────────────────────┬───────────────────┘
               │                              │
               │                              │
      Direct Queries                    Federated Queries
               │                              │
               │                              │
┌──────────────▼─────────┐       ┌───────────▼──────────────────┐
│  Database Manager      │       │   APACHE DRILL               │
│  (Direct Connections)  │       │  (Federated Query Engine)    │
│                        │       │                               │
│  - PostgreSQL Driver   │       │  Storage Plugins:             │
│  - MongoDB Driver      │       │  ├── postgres (JDBC)          │
│  - Error Handling      │       │  ├── mongo                    │
└─────────┬──────────────┘       │  └── dfs (CSV)                │
          │                      └────────┬──────────────────────┘
          │                               │
          │                               │
          └───────────┬───────────────────┘
                      │
         ┌────────────┼────────────┐
         │            │            │
┌────────▼────┐  ┌───▼─────┐  ┌──▼─────────┐
│ PostgreSQL  │  │ MongoDB │  │  CSV Files │
│             │  │         │  │            │
│ 5 Tables    │  │5 Colls  │  │ sensor_    │
│ Relational  │  │NoSQL    │  │ readings   │
└─────────────┘  └─────────┘  └────────────┘
```

---

## 🔄 REQUEST FLOW DIAGRAM

### Scenario 1: User Login

```
┌──────┐                                                  ┌──────────┐
│User  │                                                  │PostgreSQL│
└──┬───┘                                                  └────┬─────┘
   │                                                           │
   │ 1. Enter credentials                                      │
   │────────────────────────────────────┐                     │
   │                                    │                     │
   │ <login.html>                       │                     │
   │    ↓                               │                     │
   │ login.js                           │                     │
   │    ↓                               │                     │
   │ POST /api/login                    │                     │
   │────────────────────────────────────▶                     │
   │                                    │                     │
   │                              2. Verify user              │
   │                              app.py → auth.py            │
   │                                    │──────────────────▶  │
   │                                    │                     │
   │                                    │  SELECT * FROM      │
   │                                    │  User_Info WHERE... │
   │                                    │◀────────────────────│
   │                                    │                     │
   │                              3. Check password           │
   │                              bcrypt.checkpw()            │
   │                                    │                     │
   │                              4. Create session           │
   │                              session['user_id'] = ...    │
   │                                    │                     │
   │ 5. Return success                  │                     │
   │◀────────────────────────────────────                     │
   │                                                           │
   │ 6. Redirect to dashboard                                 │
   │────────────────────────────────────┐                     │
   │                                    │                     │
   │ <dashboard.html>                   │                     │
   │                                                           │
```

### Scenario 2: Execute Federated Query

```
┌──────┐                                    ┌──────┐         ┌──────────┐
│User  │                                    │Drill │         │PostgreSQL│
└──┬───┘                                    └──┬───┘         └────┬─────┘
   │                                           │                   │
   │ 1. Enter SQL query                        │                   │
   │───────────────────────┐                   │                   │
   │                       │                   │                   │
   │ <query.html>          │                   │                   │
   │    ↓                  │                   │                   │
   │ query.js              │                   │                   │
   │    ↓                  │                   │                   │
   │ POST /api/federated-query                 │                   │
   │───────────────────────▶                   │                   │
   │                       │                   │                   │
   │                  2. Validate session      │                   │
   │                  @login_required          │                   │
   │                  @role_required           │                   │
   │                       │                   │                   │
   │                  3. Send to Drill         │                   │
   │                  database.py → DrillDB    │                   │
   │                       │──────────────────▶│                   │
   │                       │                   │                   │
   │                       │  POST /query.json │                   │
   │                       │  {query: "..."}   │                   │
   │                       │                   │                   │
   │                       │            4. Parse & execute         │
   │                       │            query optimizer            │
   │                       │                   │                   │
   │                       │            5. Query data source       │
   │                       │                   │──────────────────▶│
   │                       │                   │  Execute SQL      │
   │                       │                   │◀──────────────────│
   │                       │                   │  Return rows      │
   │                       │                   │                   │
   │                       │  6. Return results                    │
   │                       │◀──────────────────│                   │
   │                       │  {rows: [...]}    │                   │
   │                       │                   │                   │
   │                  7. Log query             │                   │
   │                  INSERT INTO Query_Log────────────────────────▶
   │                       │                                       │
   │ 8. Return to frontend │                                       │
   │◀───────────────────────                                       │
   │                                                               │
   │ 9. Display results                                            │
   │ query.js → displayResults()                                   │
   │ <table with data>                                             │
   │                                                               │
```

---

## 🗄️ DATABASE SCHEMA RELATIONSHIPS

### PostgreSQL Schema

```
┌─────────────────────┐
│     User_Info       │
│─────────────────────│
│ PK user_id          │──┐
│    name             │  │
│    email            │  │
│    password_hash    │  │
│    role             │  │
│    created_at       │  │
└─────────────────────┘  │
                         │
                         │ FK
                         │
              ┌──────────▼──────────┐
              │    Query_Log        │
              │─────────────────────│
              │ PK query_id         │
              │ FK user_id          │
              │    query_text       │
              │    executed_at      │
              └─────────────────────┘

┌─────────────────────┐
│   Region_Info       │
│─────────────────────│
│ PK region_id        │──┬──────────────────┐
│    region_name      │  │                  │
│    latitude         │  │                  │
│    longitude        │  │                  │
└─────────────────────┘  │                  │
                         │ FK               │ FK
              ┌──────────▼──────────┐       │
              │   Climate_Data      │       │
              │─────────────────────│       │
              │ PK climate_id       │       │
              │ FK region_id        │       │
              │    temperature      │       │
              │    rainfall         │       │
              │    humidity         │       │
              │    timestamp        │       │
              └─────────────────────┘       │
                                            │
                                ┌───────────▼──────────┐
                                │  Agriculture_Data    │
                                │──────────────────────│
                                │ PK agri_id           │
                                │ FK region_id         │
                                │    crop_type         │
                                │    yield             │
                                │    season            │
                                │    year              │
                                └──────────────────────┘
```

### MongoDB Collections (Embedded Documents)

```
┌──────────────────────────────┐
│   Biodiversity_Data          │
├──────────────────────────────┤
│ biodiversity_id              │
│ region_id                    │
│ region_name                  │
│ species_count                │
│ endangered_species: [...]    │◀─── Array
│ dominant_flora: [...]        │◀─── Array
│ conservation_status          │
│ last_survey_date             │
└──────────────────────────────┘

┌──────────────────────────────┐
│   Species_Details            │
├──────────────────────────────┤
│ species_id                   │
│ common_name                  │
│ scientific_name              │
│ classification: {            │◀─── Embedded Object
│   kingdom: "..."             │
│   phylum: "..."              │
│   class: "..."               │
│ }                            │
│ habitat_regions: [...]       │◀─── Array
│ threats: [...]               │◀─── Array
└──────────────────────────────┘
```

---

## 🔐 AUTHENTICATION & AUTHORIZATION FLOW

```
┌────────────────────────────────────────────────────────────┐
│                    INCOMING REQUEST                         │
└────────────────────────┬───────────────────────────────────┘
                         │
                         ▼
              ┌──────────────────────┐
              │  Check Session       │
              │  session['logged_in']│
              └──────────┬───────────┘
                         │
         ┌───────────────┼───────────────┐
         │               │               │
         │ No            │ Yes           │
         ▼               ▼               │
┌─────────────────┐  ┌──────────────────▼───────┐
│ Return 401      │  │ Check User Role          │
│ Unauthorized    │  │ session['role']          │
│                 │  └──────────┬───────────────┘
│ Redirect to     │             │
│ login.html      │             │
└─────────────────┘   ┌─────────┴─────────────┐
                      │                       │
                      │                       │
         Role matches required?        Role doesn't match
                      │                       │
                      │ Yes                   │ No
                      ▼                       ▼
         ┌────────────────────────┐  ┌────────────────┐
         │  Allow Request         │  │ Return 403     │
         │  Execute Route Handler │  │ Forbidden      │
         └────────────────────────┘  └────────────────┘
```

---

## 💾 DATA STORAGE DISTRIBUTION

```
┌─────────────────────────────────────────────────────────────┐
│                   DATA DISTRIBUTION                         │
└─────────────────────────────────────────────────────────────┘

POSTGRESQL (Relational Data)
├── User Management
│   └── User_Info (authentication, roles)
├── Geographic Data
│   └── Region_Info (locations)
├── Structured Time-Series
│   ├── Climate_Data (temperature, rainfall, humidity)
│   └── Agriculture_Data (crop yields)
└── Audit Trail
    └── Query_Log (query history)

MONGODB (Semi-Structured Data)
├── Complex Nested Documents
│   ├── Biodiversity_Data (arrays of species)
│   └── Species_Details (embedded classification)
├── Event Logs
│   └── Sensor_Logs (real-time events)
├── Historical Analytics
│   └── Air_Quality_History (nested pollutants)
└── Metadata
    └── Sensor_Metadata (device information)

CSV FILES (Raw Sensor Data)
└── Real-Time Readings
    └── sensor_readings.csv (timestamp, metrics)
```

---

## 🌐 NETWORK ARCHITECTURE

```
┌─────────────────────────────────────────────────────────┐
│                    LOCALHOST                             │
│                                                          │
│  Port 5000                                              │
│  ┌──────────────────────┐                              │
│  │  Flask Backend       │                              │
│  │  (Python)            │                              │
│  └──────────────────────┘                              │
│                                                          │
│  Port 5432                                              │
│  ┌──────────────────────┐                              │
│  │  PostgreSQL          │                              │
│  │  (environmental_db)  │                              │
│  └──────────────────────┘                              │
│                                                          │
│  Port 27017                                             │
│  ┌──────────────────────┐                              │
│  │  MongoDB             │                              │
│  │  (environmental_db)  │                              │
│  └──────────────────────┘                              │
│                                                          │
│  Port 8047                                              │
│  ┌──────────────────────┐                              │
│  │  Apache Drill        │                              │
│  │  (Query Engine)      │                              │
│  └──────────────────────┘                              │
│                                                          │
└─────────────────────────────────────────────────────────┘
         ▲
         │
         │ Browser Access
         │ http://localhost:5000
         │
┌────────┴────────┐
│   USER BROWSER  │
│  (Any Browser)  │
└─────────────────┘
```

---

## 📦 COMPONENT INTERACTION MATRIX

```
┌──────────────┬──────┬──────┬──────┬───────┬─────┬─────┐
│              │Flask │Drill │PG    │Mongo  │CSV  │User │
├──────────────┼──────┼──────┼──────┼───────┼─────┼─────┤
│Flask Backend │  -   │  ✓   │  ✓   │   ✓   │  -  │  ✓  │
│Apache Drill  │  ✓   │  -   │  ✓   │   ✓   │  ✓  │  -  │
│PostgreSQL    │  ✓   │  ✓   │  -   │   -   │  -  │  -  │
│MongoDB       │  ✓   │  ✓   │  -   │   -   │  -  │  -  │
│CSV Files     │  -   │  ✓   │  -   │   -   │  -  │  -  │
│User Browser  │  ✓   │  -   │  -   │   -   │  -  │  -  │
└──────────────┴──────┴──────┴──────┴───────┴─────┴─────┘

Legend:
✓ = Direct interaction
- = No direct interaction
```

---

## 🔄 SESSION LIFECYCLE

```
┌─────────────────────────────────────────────────────────┐
│                   SESSION LIFECYCLE                      │
└─────────────────────────────────────────────────────────┘

1. User Login
   ↓
2. Verify Credentials (PostgreSQL)
   ↓
3. Create Session
   session = {
     user_id: 1,
     name: "Sarah Johnson",
     email: "sarah.researcher@example.com",
     role: "Researcher",
     logged_in: true
   }
   ↓
4. Set Session Cookie
   (Flask automatically handles)
   ↓
5. User Makes Requests
   (Cookie sent automatically)
   ↓
6. Each Request Validated
   @login_required decorator
   @role_required decorator
   ↓
7. Session Active (1 hour)
   PERMANENT_SESSION_LIFETIME = 3600
   ↓
8. User Logout OR Timeout
   ↓
9. Clear Session
   session.clear()
```

---

## 🎯 USER ROLE PERMISSIONS MAP

```
┌────────────────────┬─────────────┬──────────────┬──────────────┐
│     FEATURE        │ Researcher  │Data Provider │Administrator │
├────────────────────┼─────────────┼──────────────┼──────────────┤
│ Login/Logout       │      ✓      │      ✓       │      ✓       │
│ View Dashboard     │      ✓      │      ✓       │      ✓       │
│ Federated Queries  │      ✓      │      ✗       │      ✓       │
│ Insert Climate     │      ✗      │      ✓       │      ✓       │
│ Insert Agriculture │      ✗      │      ✓       │      ✓       │
│ Insert Sensor Log  │      ✗      │      ✓       │      ✓       │
│ Create Users       │      ✗      │      ✗       │      ✓       │
│ Delete Users       │      ✗      │      ✗       │      ✓       │
│ View Query Logs    │      ✗      │      ✗       │      ✓       │
│ DB Status Check    │      ✓      │      ✓       │      ✓       │
└────────────────────┴─────────────┴──────────────┴──────────────┘

✓ = Allowed
✗ = Denied
```

---

**Use these diagrams to understand system architecture and data flow! 📊**
