# Example .env for local development
# Copy this file to `.env` and update values (do NOT commit .env with real secrets)

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=environmental_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=pswd
# Optional: some systems use PGPASSWORD environment variable
PGPASSWORD=pswd

# Apache Drill
DRILL_HOST=localhost
DRILL_PORT=8047

# MongoDB
# MONGODB_URI=mongodb://localhost:27017/

# Groq API key (if used)
#GROQ_API_KEY=pswd
